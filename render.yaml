services:
  - type: web
    name: wordscanner-backend
    env: python
    buildCommand: |
      apt-get update && apt-get install -y npm tesseract-ocr
      python3 -m pip install --upgrade pip
      python3 -m pip install -r requirements.txt
      npm install
    startCommand: "node app.js"
